[project]
name = "universe"
version = "0.1.0"
description = "Waterloo Rocketry monorepo tooling."
requires-python = ">=3.14"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=8.0",
]
